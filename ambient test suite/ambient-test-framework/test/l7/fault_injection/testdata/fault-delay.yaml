apiVersion: networking.istio.io/v1
kind: VirtualService
metadata:
  name: podinfo-fault-delay
  namespace: {{ .Namespace }}
  labels:
    test-id: {{ .TestID }}
spec:
  hosts:
    - podinfo.{{ .Namespace }}.svc.cluster.local
  gateways:
    - mesh
  http:
    - fault:
        delay:
          percentage:
            value: 100.0
          fixedDelay: 3s
      route:
        - destination:
            host: podinfo.{{ .Namespace }}.svc.cluster.local
